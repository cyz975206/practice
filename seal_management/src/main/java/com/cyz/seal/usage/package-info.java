/**
 * 用印申请上下文（Usage context）—— 用印申请 / 用印方式 / 履行 / 使用记录。
 *
 * <p>状态机：草稿 → 审批中 → {已拒绝|已撤销|已审批} → 履行中（含设备异步子状态）→ {已完成|失败/超时}。
 * 履行完成混合驱动（设备回调 / 保管员手动确认）；借用超时三档阶梯转遗失。详见 CONTEXT.md。
 */
package com.cyz.seal.usage;
